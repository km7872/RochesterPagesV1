CREATE TABLE [dbo].[SiteInfo] (
    [Site_ID]      VARCHAR (12) NOT NULL,
    [SiteName]     VARCHAR (50) NOT NULL,
    [Site_LocCode] VARCHAR (4)  NOT NULL,
    PRIMARY KEY CLUSTERED ([Site_ID] ASC)
);

